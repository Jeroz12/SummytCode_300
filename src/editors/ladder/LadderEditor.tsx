/**
 * Editor Ladder — placeholder.
 * El canvas interactivo (React Flow / Konva) llega en la Fase 2.
 * La estructura de datos y el traductor a AST ya existen en compiler-core/src/ladder/.
 */
export function LadderEditor() {
  return (
    <div className="placeholder">
      <div className="placeholder__icon">⚡</div>
      <div>Editor Ladder</div>
      <div style={{ maxWidth: 360, fontSize: 12 }}>
        El canvas gráfico de contactos y bobinas se implementará en la Fase 2. Por ahora,
        usa la pestaña <strong>ST</strong> para escribir lógica.
      </div>
    </div>
  );
}
